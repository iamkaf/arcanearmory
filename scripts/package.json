{
  "name": "scripts",
  "version": "1.0.0",
  "description": "",
  "main": "generate_textures.js",
  "scripts": {
    "start": "ts-node --transpile-only ./texture-generator",
    "translate": "ts-node --transpile-only ./translation-helper",
    "clean": "rimraf ./texture-generator/generated/resources",
    "generate-all": "npm run start && npm run translate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "change-case": "^5.4.3",
    "change-case-all": "^2.1.0",
    "mkdirp": "^3.0.1",
    "sharp": "^0.33.2",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}
